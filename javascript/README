poc.html reflects the current code.

DONE - poc phase two:
DONE - extend one-pager to handle an authenticated call (in this case, to the friends timeline), going through Apigee as authentication mediator
DONE - store Apigee credentials as HTML5 local storage (if browser supports, if not, force user to authenticate each time)

- poc three:
IN PROCESS (ALL GET METHODS WORK; BACKEND CODE ISN'T SUPPORTING OTHER VERBS YET) - allow any console-supported Twitter call to be made
DONE - add parameter to jumpstart call that allows Twitter response to be returned as "raw" (unformatted)

DONE - poc four:
DONE - allow user to specify a number of HTML constructs to be returned (ul, ol, dl, and nested divs)